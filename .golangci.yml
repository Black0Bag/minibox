# golangci-lint 配置
# minibox 后端静态检查规则
run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - revive
    - misspell
    - gocritic

issues:
  exclude-use-default: false
